#!/usr/bin/env sh
#
# Ping Identity DevOps - Docker Build Hooks
#
${VERBOSE} && set -x

echo "Moving configuration.yml file"
mv /opt/in/tmp/configuration.yml /opt/out/instance/config/configuration.yml
cat /opt/out/instance/config/configuration.yml
